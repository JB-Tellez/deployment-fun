setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
  command:
    - mkdir static
    - python manage.py collectstatic --noinput
run:
  web: gunicorn blogapi_project.wsgi
